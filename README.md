📂 Python File Organizer
🚀 Project Overview
This project is a simple Python automation script that automatically organizes files in a directory into categorized folders based on their file extensions.
It scans the current working directory and moves files into folders such as PDFs, Word Files, Excel Files, Images, Audio, Video, and Python Files.

🛠️ Technologies Used
Python 3
os module (File & Directory handling)
shutil module (File movement operations)

📁 Features
✔ Automatically creates folders if they do not exist
✔ Detects file extensions dynamically
✔ Moves files into appropriate folders
✔ Skips existing folders
✔ Uses dictionary-based file mapping for clean logic
